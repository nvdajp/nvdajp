version: "{branch}-{build}"

branches:
 only:
  - jpbeta

environment:
 PY_PYTHON: 2.7-32

clone_depth: 1

install:
 - git submodule update --init

build_script:
 - jptools\nonCertAllBuild.cmd

artifacts:
 - path: output\*
