version: "{branch}-{build}"

branches:
 only:
  - jpbeta

environment:
 PY_PYTHON: 2.7-32

clone_depth: 1

install:
 - PATH %PATH%;C:\MinGW;C:\MinGW\bin;C:\MinGW\msys\1.0
 - git submodule update --init

build_script:
 - jptools\nonCertAllBuild.cmd

artifacts:
 - path: output\*
