# NVDA日本語版 開発ガイドライン for Claude

このファイルは Claude Code がこのリポジトリで作業する際の重要な指示を記載しています。

## Git コミットのガイドライン

### CI のスキップ

ドキュメントのみの変更をコミットする場合は、コミットメッセージに `[skip ci]` を含めてください：

```bash
git commit -m "docs: Update README [skip ci]"
```

これにより AppVeyor がビルドをスキップし、CI リソースを節約できます。

### 適用する場合

- README やドキュメントのみの変更
- コメントの追加・修正のみ
- .gitignore や設定ファイルの軽微な変更

### 適用しない場合

- ソースコードの変更
- ビルドスクリプトの変更
- テストコードの追加・修正

## プロジェクト固有の情報

### ブランチ戦略

- `betajp-` で始まるブランチ名を使用すると、CI でコード署名なしのビルドが実行される
- プルリクエストは `betajp` ブランチに対して行う

### ビルドシステム

- サブモジュール入れ子構造は PR #492 で解消済み
- `copy_jtalk_core_files.cmd` はサブモジュール構造変更の結果として必要
- ビルドスクリプトの最適化は段階的に実施中

## その他の注意事項

- ファイル改行コードは Windows 環境のため CRLF を使用
- 日本語コメントは適切に使用可能
- システムテストは日本語環境で実行される
