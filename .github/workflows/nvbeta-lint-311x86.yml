name: NVAccess Beta Aligned Lint (3.11 x86)

on:
  push:
    branches:
      - alphajp*
      - betajp*
      - releasejp*
  pull_request:
    branches:
      - alphajp*
      - betajp*
      - releasejp*
  workflow_dispatch:

jobs:
  lint:
    name: Ruff format and lint
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Python 3.11 x86
        uses: actions/setup-python@v5
        with:
          python-version: '3.11.9'
          architecture: 'x86'

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Run ruff format --check (advisory)
        continue-on-error: true
        shell: pwsh
        run: uv run ruff format --check

      - name: Run ruff check (advisory)
        continue-on-error: true
        shell: pwsh
        run: uv run ruff check
