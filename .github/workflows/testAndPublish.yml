name: CI/CD Japanese Version

on:
  push:
    branches:
      - 'betajp'
      - 'releasejp'
  pull_request:
    branches:
      - 'alphajp**'
      - 'betajp**'
      - 'releasejp**'
  workflow_dispatch:

env:
  PY_PYTHON: 3.11-32
  RELEASE: 1
  PUBLISHER: nvdajp
  # Cache details about available MSVC tooling for subsequent SCons invocations
  SCONS_CACHE_MSVC_CONFIG: ".scons_msvc_cache.json"
  # BEGIN JP PATCH: Allow overriding Windows runner label via repo variables
  # Set repository variable JP_WINDOWS_RUNNER to switch runner, default to windows-latest.
  # This helps when windows-2025 is unavailable on the target environment.
  # END JP PATCH

jobs:
  typeCheck:
    name: Static type analysis (pyright)
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Set up Python 3.11 x86
      uses: actions/setup-python@v5
      with:
        python-version: '3.11.9'
        architecture: 'x86'

    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true

    - name: Run type check (pyright)
      shell: pwsh
      run: ci/scripts/tests/typeCheck.ps1

  lint:
    name: Ruff format and lint (advisory)
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Set up Python 3.11 x86
      uses: actions/setup-python@v5
      with:
        python-version: '3.11.9'
        architecture: 'x86'

    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true

    - name: Run ruff format --check (advisory)
      continue-on-error: true
      shell: pwsh
      run: uv run ruff format --check

    - name: Run ruff check (advisory)
      continue-on-error: true
      shell: pwsh
      run: uv run ruff check

  buildNVDA:
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    outputs:
      version: ${{ steps.releaseInfo.outputs.VERSION }}
      versionType: ${{ steps.releaseInfo.outputs.VERSION_TYPE }}
      APIVersion: ${{ steps.releaseInfo.outputs.NVDA_API_VERSION }}
      APIBackCompat: ${{ steps.releaseInfo.outputs.NVDA_API_COMPAT_TO }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 1

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'

    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true

    - name: Setup MSVC environment
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x86

    - name: Configure git
      run: |
        git config --global core.autocrlf true
        python --version
        uv --version
        nmake /? | Select-Object -First 1

    - name: Set cache key for SCons MSVC Cache
      shell: powershell
      run: |
        echo "SCONS_CACHE_KEY=$env:RUNNER_OS-$env:ImageVersion" >> $env:GITHUB_ENV

    - name: SCons MSVC Cache
      uses: actions/cache@v4
      with:
        path: ${{ env.SCONS_CACHE_MSVC_CONFIG }}
        key: ${{ env.SCONS_CACHE_KEY }}

    - name: Set version variables
      run: |
        $baseVersion = "2025.1.2jp"
        
        # Set timezone and get current date
        tzutil /s "Tokyo Standard Time"
        $nowDate = python jptools/nowdate.py
        $updateVersionType = "nvdajp"
        
        if ($env:GITHUB_REF_NAME.StartsWith("alphajp")) {
            $version = "jpalpha_$nowDate"
            $updateVersionType += "alpha"
        } elseif ($env:GITHUB_REF_NAME.StartsWith("betajp")) {
            $version = "$baseVersion-beta-$nowDate"
            $updateVersionType += "beta"
        } elseif ($env:GITHUB_REF_NAME.StartsWith("releasejp")) {
            $version = "$baseVersion-$nowDate"
        } else {
            $version = $baseVersion
        }
        
        echo "VERSION=$version" >> $env:GITHUB_ENV
        echo "UPDATEVERSIONTYPE=$updateVersionType" >> $env:GITHUB_ENV
        echo "Building version: $version ($updateVersionType)"
      shell: powershell

    - name: Install dependencies
      run: |
        python -m pip install --upgrade --no-warn-script-location pip

    - name: Get API version info
      run: |
        $apiVersion = python -c "import sys; sys.path.append('source'); from addonAPIVersion import CURRENT; print('{}.{}.{}'.format(*CURRENT))"
        $apiCompatTo = python -c "import sys; sys.path.append('source'); from addonAPIVersion import BACK_COMPAT_TO; print('{}.{}.{}'.format(*BACK_COMPAT_TO))"
        echo "API Version: $apiVersion"
        echo "API Back Compat To: $apiCompatTo"
      shell: powershell

    - name: Build NVDA Japanese version (SCons dist+launcher)
      shell: cmd
      run: |
        setlocal
        set USE_PY_OVERLAY_COPY=1
        set sconsArgs=version_build=%GITHUB_RUN_NUMBER% --all-cores
        echo Building with args: %sconsArgs% (USE_PY_OVERLAY_COPY=%USE_PY_OVERLAY_COPY%)
        scons dist launcher %sconsArgs%
        if errorlevel 1 exit /b %errorlevel%

  # BEGIN JP PATCH: Optional self-signed build for CI (disabled by default)
  jpSelfSignedBuild:
    name: JP Self-signed build (optional)
    if: ${{ vars.JP_ENABLE_SELF_SIGN_CI == '1' }}
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true
    - name: Set up Python 3.11 x86
      uses: actions/setup-python@v5
      with:
        python-version: '3.11.9'
        architecture: 'x86'
    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true
    - name: Setup MSVC environment
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x86
    - name: Create self-signed code-signing certificate
      shell: pwsh
      run: ci/scripts/tests/selfSignedCert.ps1 -Mode create -PfxPath "$PWD/selfsigned.pfx" -Subject "CN=NVDA-JP CI SelfSigned"
    - name: SCons build (signed with self-signed cert)
      shell: cmd
      run: |
        setlocal
        set USE_PY_OVERLAY_COPY=1
        set sconsArgs=version_build=%GITHUB_RUN_NUMBER% --all-cores certFile=%SELF_SIGNED_PFX% certTimestampServer=http://timestamp.digicert.com
        scons dist launcher %sconsArgs%
        if errorlevel 1 exit /b %errorlevel%
    - name: Cleanup self-signed certificate
      if: always()
      shell: pwsh
      run: ci/scripts/tests/selfSignedCert.ps1 -Mode cleanup -Thumbprint "$env:SELF_SIGNED_THUMBPRINT"
  # END JP PATCH

    - name: Build Japanese addons (no 7z)
      shell: pwsh
      run: |
        $env:VERSION = "${{ env.VERSION }}"
        uv run python jptools/pack_jtalk_addon.py --nowdate $env:VERSION
        uv run python jptools/pack_kgs_addon.py --version $env:VERSION
      env:
        VERSION: ${{ env.VERSION }}

    # Controller client zip is produced via SCons in createLauncher job (alias: client)

    - name: Test miscDepsJp
      run: |
        call jptools\testMiscDepsJp.cmd
      shell: cmd

    - name: Run Japanese tests
      run: |
        call jptools\tests.cmd
        call jpchar\tests.cmd
      shell: cmd

    - name: Prepare for system tests
      run: |
        # Create test output directories using the same script as upstream
        ci/scripts/tests/beforeTests.ps1
        
        # Start Chrome for system tests
        $chromeStartArgsString = $(py tests/system/libraries/_chromeArgs.py)
        $chromeStartArgsArray = $chromeStartArgsString -split " "
        cmd /c start /min $chromeStartArgsArray
        
        echo "testFailExitCode=0" >> $env:GITHUB_ENV
      shell: powershell

    - name: Install NVDA for testing
      run: |
        # Discover the built installer rather than assuming the exact name.
        $candidate = Get-ChildItem -Path .\output -Filter "nvda_*.exe" -File -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1
        if (-not $candidate) {
          echo "::error::No NVDA installer found under .\\output. Listing directory for diagnostics..."
          Get-ChildItem -Path .\output -Force -ErrorAction SilentlyContinue | Format-List | Out-String -Width 2000
          exit 1
        }
        $nvdaLauncherFile = $candidate.FullName
        echo "nvdaLauncherFile=$nvdaLauncherFile" >> $env:GITHUB_ENV
        echo "NVDALauncherFile: $nvdaLauncherFile"

        $outputDir = $(resolve-path .\testOutput)
        $installerLogFilePath = "$outputDir\nvda_install.log"
        $installerProcess = start-process -FilePath "$nvdaLauncherFile" -ArgumentList "--install-silent --debug-logging --log-file $installerLogFilePath" -passthru
        
        try {
          $installerProcess | wait-process -Timeout 180 -ErrorAction Stop
          $errorCode = $installerProcess.ExitCode
        } catch {
          echo "::error::NVDA installer process timed out"
          $errorCode = 1
        }
        
        if($errorCode -ne 0) { 
          echo "::error::NVDA installation failed with exit code $errorCode"
          exit $errorCode 
        }
      shell: powershell

    - name: Run unit tests
      run: |
        $outDir = (Resolve-Path .\testOutput\unit\)
        $unitTestsXml = "$outDir\unitTests.xml"
        .\rununittests.bat --output-file "$unitTestsXml" -v
        
        if($LastExitCode -ne 0) {
          echo "testFailExitCode=$LastExitCode" >> $env:GITHUB_ENV
          echo "::error::Unit tests failed"
        } else {
          echo "::notice::Unit tests passed"
        }
      shell: powershell

    - name: Run system tests
      run: |
        $testOutput = (Resolve-Path .\testOutput\)
        $systemTestOutput = (Resolve-Path "$testOutput\system")
        
        $verboseDebugLogging = if ($env:VERBOSE_SYSTEM_TEST_LOGGING) { "True" } else { "" }
        # Skip symbolPronunciation tests due to timeout issues in Japanese version
        $tagsForTest = "installer startupShutdown"
        
        if ($env:INCLUDE_SYSTEM_TEST_TAGS) {
          if ($env:INCLUDE_SYSTEM_TEST_TAGS -eq "excluded_from_build") {
            echo "::notice::System tests skipped"
            return
          }
          $tagsForTest = $env:INCLUDE_SYSTEM_TEST_TAGS
        }
        
        $tagsForTestArray = -split $tagsForTest
        $includeTags = $tagsForTestArray | ForEach-Object { "--include", $_ }
        
        .\runsystemtests.bat --variable whichNVDA:installed --variable installDir:"$env:nvdaLauncherFile" --variable verboseDebugLogging:"$verboseDebugLogging" @includeTags
        
        if($LastExitCode -ne 0) {
          echo "testFailExitCode=$LastExitCode" >> $env:GITHUB_ENV
          echo "::error::System tests failed (tags: $tagsForTest)"
        } else {
          echo "::notice::System tests passed (tags: $tagsForTest)"
        }
        
        Compress-Archive -Path "$systemTestOutput\*" -DestinationPath "$testOutput\systemTestResult.zip"
      shell: powershell
      env:
        # On GitHub Actions, avoid installer tests (unsigned build). Run startup/shutdown only.
        INCLUDE_SYSTEM_TEST_TAGS: startupShutdown

    - name: Check test results
      run: |
        if ($env:testFailExitCode -and $env:testFailExitCode -ne "0") {
          echo "::error::Tests failed with exit code $env:testFailExitCode"
          exit $env:testFailExitCode
        } else {
          echo "::notice::All tests passed"
        }
      shell: powershell

    - name: Cache scons build
      uses: actions/cache/save@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
    - name: Store release metadata information
      id: releaseInfo
      run: |
        Write-Output VERSION=$env:VERSION >> $env:GITHUB_OUTPUT
        Write-Output VERSION_TYPE=$env:UPDATEVERSIONTYPE >> $env:GITHUB_OUTPUT
        Write-Output NVDA_API_VERSION=$(python -c "import sys; sys.path.append('source'); from addonAPIVersion import CURRENT; print('{}.{}.{}'.format(*CURRENT))") >> $env:GITHUB_OUTPUT
        Write-Output NVDA_API_COMPAT_TO=$(python -c "import sys; sys.path.append('source'); from addonAPIVersion import BACK_COMPAT_TO; print('{}.{}.{}'.format(*BACK_COMPAT_TO))") >> $env:GITHUB_OUTPUT

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: nvda-jp-build-${{ github.run_number }}
        path: |
          output/**/*
          testOutput/**/*
          miscDepsJp/__mypy.txt
          miscDepsJp/jptools/__h1output.txt
          miscDepsJp/jptools/__h2output.txt
          miscDepsJp/jptools/__test_log.txt
          miscDepsJp/jptools/__jpBrailleHarness.md
          miscDepsJp/jptools/jpBrailleHarness.html
          jptools/__jpdictest_log.txt
          jptools/nvdajp-jtalk-*.nvda-addon
          jptools/kgsbraille-*.nvda-addon
          jpchar/__checkchardesc_log.txt
        retention-days: 30

  checkPo:
    name: Check po files for errors
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # Include gettext
        submodules: true
    - name: Set up python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install pre-commit
      run: |
        python -m pip install --upgrade pip
        pip install pre-commit
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Run pre-commit
      run: |
        # Ensure uv environment is up to date.
        # If the uv environment is outdated, running pre-commit will trigger uv to generate a uv.lock file,
        # which causes checkPo to fail without clear errors because pre-commit fails when files are changed during its run.
        pre-commit run uv-lock --all-files
        # Run pre-commit on the translations
        pre-commit run checkPo --all-files
    - name: Add job summary
      if: ${{ failure() }}
      shell: bash
      run: |
        echo "PO files contain errors. Please fix them before merging." >> $GITHUB_STEP_SUMMARY

  checkPot:
    name: Check translator comments
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: buildNVDA
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Check comments for translators
      run: ci/scripts/tests/translationCheck.ps1
    - name: Upload artifact
      if: ${{ success() || failure() }}
      uses: actions/upload-artifact@v4
      with:
        name: pot-check-${{ github.run_number }}
        path: testOutput/translationCheck.txt

  licenseCheck:
    name: Check license compatibility of dependencies
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: buildNVDA
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: License check
      run: ci/scripts/tests/licenseCheck.ps1

  unitTests:
    name: Run unit tests
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: buildNVDA
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Run unit tests
      run: ci/scripts/tests/unitTests.ps1
    - name: Replace relative paths in unit test results
      if: ${{ failure() }}
      # Junit reports fail on these
      shell: bash
      run: sed -i 's|file="..\\|file="|g' testOutput/unit/unitTests.xml
    - name: Upload artifact
      if: ${{ failure() }}
      uses: actions/upload-artifact@v4
      with:
        name: Unit tests results
        path: testOutput/unit/unitTests.xml
    - name: Publish unit test report
      uses: mikepenz/action-junit-report@v5
      if: ${{ failure() }}
      with:
        check_name: Unit tests
        detailed_summary: true
        annotate_only: true
        report_paths: testOutput/unit/unitTests.xml

  # BEGIN JP PATCH: Disable upstream Crowdin upload job for nvdajp
  crowdinUpload:
    if: ${{ false }}
    name: Upload translations to Crowdin (disabled in JP)
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: [buildNVDA, checkPot]
    steps:
      - name: Skip
        run: echo "Crowdin upload is disabled for nvdajp."
  # END JP PATCH

  createLauncher:
    name: Create launcher
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: buildNVDA
    outputs:
      launcherArtifactId: ${{ steps.uploadLauncher.outputs.artifact-id }}
      launcherSha256: ${{ steps.addJobSummary.outputs.SHA256 }}
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Set version variables from buildNVDA
      run: |
        # Use version from buildNVDA job
        echo "version=${{ needs.buildNVDA.outputs.version }}" >> $env:GITHUB_ENV
        echo "versionType=${{ needs.buildNVDA.outputs.versionType }}" >> $env:GITHUB_ENV
        echo "UPDATEVERSIONTYPE=${{ needs.buildNVDA.outputs.versionType }}" >> $env:GITHUB_ENV
        echo "PUBLISHER=nvdajp" >> $env:GITHUB_ENV
        echo "release=1" >> $env:GITHUB_ENV
        echo "Using version: ${{ needs.buildNVDA.outputs.version }}"
      shell: powershell
    - name: Set scons args
      run: ci/scripts/setSconsArgs.ps1
      env:
        apiSigningToken: ${{ github.event_name == 'push' && secrets.API_SIGNING_TOKEN || '' }}
    - name: Build scons docs targets
      shell: cmd
      run: scons %sconsDocsOutTargets% %sconsArgs% %sconsCores%

    - name: Build packaging metadata and client zip (no launcher rebuild)
      shell: cmd
      # Avoid rebuilding the launcher; use cached exe from buildNVDA
      run: |
        scons client moduleList %sconsArgs% %sconsCores%

    - name: Verify cached launcher exists
      shell: powershell
      run: |
        $candidate = Get-ChildItem -Path .\output -Filter "nvda_*.exe" -File -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1
        if (-not $candidate) {
          echo "::error::No cached NVDA launcher found under .\\output. Ensure buildNVDA produced the installer."
          Get-ChildItem -Path .\output -Force -ErrorAction SilentlyContinue | Format-List | Out-String -Width 2000
          exit 1
        }
        echo "Using cached launcher: $($candidate.Name)"
    - name: Upload launcher
      id: uploadLauncher
      uses: actions/upload-artifact@v4
      with:
        name: NVDA launcher
        path: |
          output/nvda*.exe
          output/nvda*controllerClient.zip
    - name: Upload documentation artifacts
      uses: actions/upload-artifact@v4
      id: uploadBuildArtifacts
      with:
        name: Documentation files and packaging metadata
        path: |
          output/*.html
          output/*.css
          output/library_modules.txt
          output/installed_python_packages.txt
    - name: Add job summary
      id: addJobSummary
      shell: bash
      run: |
        NVDA_EXE_NAME=$(ls output/nvda*.exe | head -n 1)
        SHA256=$(sha256sum "$NVDA_EXE_NAME" | cut -d ' ' -f 1)
        echo SHA256=$SHA256 >> $GITHUB_OUTPUT
        echo "* [Download the NVDA launcher](${{ steps.uploadLauncher.outputs.artifact-url }})" >> $GITHUB_STEP_SUMMARY
        echo "    * SHA256 of launcher exe file: \`$SHA256\`" >> $GITHUB_STEP_SUMMARY
        echo "* [Download the documentation and other build artifacts](${{ steps.uploadBuildArtifacts.outputs.artifact-url }})" >> $GITHUB_STEP_SUMMARY
        echo "    * User guide, change log, developer guide" >> $GITHUB_STEP_SUMMARY
        echo "    * Packaging metadata: library modules, installed python packages" >> $GITHUB_STEP_SUMMARY

  systemTests:
    strategy:
      fail-fast: false
      matrix:
        # To skip tests, and just run install, replace with [excluded_from_build]
        # Skip chrome tests temporarily - may have issues with Japanese version
        testSuite: [installer, startupShutdown]
    name: Run system tests
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: createLauncher
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Get NVDA launcher
      id: getLauncher
      uses: actions/download-artifact@v4
      with:
        name: NVDA launcher
        path: output
    - name: Install NVDA
      run: ci/scripts/installNVDA.ps1
      env:
        nvdaLauncherDir: ${{ steps.getLauncher.outputs.download-path }}
    - name: Run system tests
      run: ci/scripts/tests/systemTests.ps1
      env:
        nvdaLauncherDir: ${{ steps.getLauncher.outputs.download-path }}
        # On GitHub Actions, always skip 'installer' by mapping to an exclusion tag understood by the script
        INCLUDE_SYSTEM_TEST_TAGS: ${{ matrix.testSuite == 'installer' && 'excluded_from_build' || matrix.testSuite }}
    - name: Upload system tests results
      if: ${{ failure() }}
      uses: actions/upload-artifact@v4
      with:
        name: "System tests results (${{ matrix.testSuite }})"
        path: |
          testOutput/system
          testOutput/install
          !testOutput/install/nvda_install_temp.log
    - name: Publish system test report
      uses: mikepenz/action-junit-report@v5
      if: ${{ failure() }}
      with:
        check_name: "System tests (${{ matrix.testSuite }})"
        detailed_summary: true
        annotate_only: true
        report_paths: testOutput/system/systemTests.xml

  createSymbols:
    name: Create symbols
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: buildNVDA
    outputs:
      symbolsArtifactId: ${{ steps.uploadArtifact.outputs.artifact-id }}
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Install Windows SDK
      run: choco install -y windows-sdk-10.1
    - name: Create build symbols
      env:
        symStore: C:\Program Files (x86)\Windows Kits\10\Debuggers\x64\symstore.exe
      run: ci/scripts/buildSymbolStore.ps1
    - name: Upload symbols artifact
      id: uploadArtifact
      uses: actions/upload-artifact@v4
      with:
        name: Symbols
        path: output/symbols.zip

  allTestsPass:
    # Is a no-op action.
    # Rulesets require specifying every job you wish to block a PR/push.
    # Used to protect branches/tags as a general status check so we can check a single job for all rulesets.
    name: Check if all tests pass
    runs-on: ubuntu-latest
    needs: [buildNVDA, checkPo, checkPot, typeCheck, licenseCheck, unitTests, createLauncher, systemTests, createSymbols]
    steps:
    - name: Check if all tests pass
      run: echo "All tests passed successfully."

  uploadSymbols:
    name: Upload symbols
    # BEGIN JP PATCH: Use overridable Windows runner (default windows-latest)
    runs-on: ${{ vars.JP_WINDOWS_RUNNER || 'windows-latest' }}
    # END JP PATCH
    needs: createSymbols
    if: ${{ github.event_name == 'push' && vars.feature_uploadSymbolsToMozilla }}
    steps:
    - name: Checkout cached build
      uses: actions/cache/restore@v4
      with:
        path: ${{ github.workspace }}
        key: ${{ github.ref }}-${{ github.run_id }}
        fail-on-cache-miss: true
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        architecture: 'x86'
    - name: Install the latest version of uv
      uses: astral-sh/setup-uv@v6
    - name: Get symbols
      uses: actions/download-artifact@v4
      with:
        name: Symbols
        path: output
    - name: Upload symbols to Mozilla
      continue-on-error: true
      run: uv run --with requests --directory ${{ github.workspace }} ci\scripts\mozillaSyms.py
      env:
        mozillaSymsAuthToken: ${{ secrets.MOZILLA_SYMS_TOKEN }}
    - name: Warn on failure
      if: ${{ failure() }}
      shell: bash
      run: |
        echo "Warning: Uploading symbols to Mozilla failed." >> $GITHUB_STEP_SUMMARY

  cleanupCache:
    # Cache deletion on pull_request events cannot occur on PRs from forks
    # as PRs from forks do not get write permissions from pull_request events.
    # Alternatively, we can change the event trigger to pull_request_target,
    # however that might introduce security risks as it grants fork PRs greater permissions.
    # In these cases we can always let GitHub handle cache deletion:
    # auto deletion after 7 days or limits are hit
    name: Cleanup cache
    permissions:
      actions: write
    # BEGIN JP PATCH: remove dependency on disabled crowdinUpload
    needs: [allTestsPass, uploadSymbols]
    # END JP PATCH
    if: ${{ always() && (github.event_name == 'push' || github.event.pull_request.head.repo.owner == github.repository_owner) }}
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup cache
        shell: bash
        run: gh cache delete ${{ github.ref }}-${{ github.run_id }} || echo "Cache not found or already deleted"
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}

  deploySnapshot:
    name: Deploy NVDA snapshot
    permissions:
      contents: write
      deployments: write
    runs-on: ubuntu-latest
    needs: [buildNVDA, createLauncher, createSymbols, allTestsPass]
    if: ${{ github.event_name == 'push' && !startsWith(github.ref_name, 'release-') }}
    concurrency:
      group: snapshot
      cancel-in-progress: true
    environment:
      name: snapshot
    steps:
    - name: Get NVDA launcher
      uses: actions/download-artifact@v4
      with:
        name: NVDA launcher
        path: output
    - name: Deploy snapshot
      shell: bash
      id: deploySnapshot
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        NVDA_EXE_NAME=$(ls output/nvda*.exe | head -n 1)
        NVDA_HASH=$(sha1sum "$NVDA_EXE_NAME" | cut -d ' ' -f 1)

        gh api \
          --method POST \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          /repos/${{ github.repository }}/deployments \
          -f "task=release" \
          -f "description=Release of $NVDA_EXE_NAME" \
          -f "ref=${{ github.ref_name }}" \
          -F "auto_merge=false" \
          -f "environment=snapshot" \
          -f "required_contexts[]" \
          -f "payload[NVDA_HASH]=$NVDA_HASH" \
          -f "payload[NVDA_ARTIFACT_ID]=${{ needs.createLauncher.outputs.launcherArtifactId }}" \
          -f "payload[SYMBOLS_ARTIFACT_ID]=${{ needs.createSymbols.outputs.symbolsArtifactId }}" \
          -f "payload[NVDA_VERSION]=${{ needs.buildNVDA.outputs.version }}" \
          -f "payload[NVDA_VERSION_TYPE]=${{ needs.buildNVDA.outputs.versionType }}" \
          -f "payload[NVDA_API_VERSION]=${{ needs.buildNVDA.outputs.APIVersion }}" \
          -f "payload[NVDA_API_COMPAT_TO]=${{ needs.buildNVDA.outputs.APIBackCompat }}"

  release:
    name: Release NVDA
    permissions:
      contents: write
      deployments: write
      discussions: write
    runs-on: ubuntu-latest
    needs: [buildNVDA, createLauncher, createSymbols, allTestsPass]
    if: startsWith(github.ref_name, 'release-')
    concurrency:
      group: release
      cancel-in-progress: true
    environment:
      name: production
    steps:
    - name: Get normalized tag names
      id: getReleaseNotes
      shell: bash
      run: |
        echo RELEASE_NAME=$GITHUB_REF_NAME | sed 's/release-//g' >> $GITHUB_OUTPUT
        echo NORMALIZED_TAG_NAME=$GITHUB_REF_NAME | sed 's/\./-/g' | sed 's/release-//g' >> $GITHUB_OUTPUT
    - name: Get NVDA launcher
      uses: actions/download-artifact@v4
      with:
        name: NVDA launcher
        path: output
    - name: VirusTotal Scan
      id: virusTotal
      uses: crazy-max/ghaction-virustotal@v4
      with:
        vt_api_key: ${{ secrets.VT_API_KEY }}
        files: output/nvda*.exe
    - name: Get normalized VT url
      id: getVTUrl
      shell: bash
      run: |
        vtUrl=$(echo ${{ steps.virusTotal.outputs.analysis }} | sed -E 's/([^=]*)=([^,]*).*/\2/')
        echo VT_URL=$vtUrl >> $GITHUB_OUTPUT
    - name: Deploy release
      shell: bash
      id: deployRelease
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        NVDA_EXE_NAME=$(ls output/nvda*.exe | head -n 1)
        NVDA_HASH=$(sha1sum "$NVDA_EXE_NAME" | cut -d ' ' -f 1)

        gh api \
          --method POST \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          /repos/${{ github.repository }}/deployments \
          -f "task=release" \
          -f "description=Release of $NVDA_EXE_NAME" \
          -f "ref=${{ github.ref_name }}" \
          -F "auto_merge=false" \
          -f "environment=production" \
          -f "required_contexts[]" \
          -f "payload[NVDA_HASH]=$NVDA_HASH" \
          -f "payload[NVDA_ARTIFACT_ID]=${{ needs.createLauncher.outputs.launcherArtifactId }}" \
          -f "payload[SYMBOLS_ARTIFACT_ID]=${{ needs.createSymbols.outputs.symbolsArtifactId }}" \
          -f "payload[NVDA_VERSION]=${{ needs.buildNVDA.outputs.version }}" \
          -f "payload[NVDA_VERSION_TYPE]=${{ needs.buildNVDA.outputs.versionType }}" \
          -f "payload[NVDA_API_VERSION]=${{ needs.buildNVDA.outputs.APIVersion }}" \
          -f "payload[NVDA_API_COMPAT_TO]=${{ needs.buildNVDA.outputs.APIBackCompat }}"
    - name: Create release notes
      id: createReleaseNotes
      shell: bash
      run: |
        echo "* Highlights can be found in the [release blog post](https://www.nvaccess.org/post/nvda-${{ steps.getReleaseNotes.outputs.NORMALIZED_TAG_NAME }}/)." >> /tmp/releaseNotes.md
        echo "* [VirusTotal scan results](${{ steps.getVTUrl.outputs.VT_URL }})" >> /tmp/releaseNotes.md
        echo "* SHA256 sum: \`${{ needs.createLauncher.outputs.launcherSha256 }}\`" >> /tmp/releaseNotes.md
    - name: Publish pre-release
      if: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') }}
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release create ${{github.ref_name }} \
          --repo ${{ github.repository }} \
          --prerelease \
          --title "${{ steps.getReleaseNotes.outputs.RELEASE_NAME }}" \
          --notes-file /tmp/releaseNotes.md \
          --verify-tag
    - name: Publish stable release
      if: ${{ !contains(github.ref_name, 'rc') && !contains(github.ref_name, 'beta') }}
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release create ${{github.ref_name }} \
          --repo ${{ github.repository }} \
          --title "${{ steps.getReleaseNotes.outputs.RELEASE_NAME }}" \
          --notes-file /tmp/releaseNotes.md \
          --verify-tag \
          --discussion-category Releases
        gh release upload ${{github.ref_name }} \
            --repo ${{ github.repository }} \
            --clobber \
            output/nvda*.exe#Installer
